#include<stdio.h>
#include<string.h>
int is_anagram(char a[],char b[]){
    if(strlen(a)!=strlen(b)){
      return 0;
    }
    int i=0;
    int freq_a[26]={0};
    int freq_b[26]={0};
}
int main(){

}